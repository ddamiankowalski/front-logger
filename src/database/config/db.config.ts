import { DatabaseConfig } from "../interfaces/dbconfig.interface";

export const config: DatabaseConfig = {
    host: 'localhost',
    user: 'admin',
    password: 't4jn3h4slo',
    database: 'frontlogger',
    dialect: 'mysql'
}